- name: Install nginx
  ansible.builtin.yum:
    name: nginx
    state: latest
- name: Make sure a service unit is running
  ansible.builtin.systemd:
    state: restarted
    name: nginx
    enabled: yes
