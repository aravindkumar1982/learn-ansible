- name: install Nginx
  hosts: all
  become: true
  tasks:
    - name: Install nginx
      ansible.builtin.yum:
        name: https
        state: latest

    - name: Start nginx
      ansible.builtin.systemd:
        state: started
        name: nginx
        enabled: yes
